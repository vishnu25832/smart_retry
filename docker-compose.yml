version: "3.9"

services:
  smart-retry-queue:
    build: .
    container_name: smart-retry-queue

    ports:
      - "5000:5000"

    environment:
      DATABASE_URL: "file:./dev.db"
      PORT: 5000

    restart: unless-stopped